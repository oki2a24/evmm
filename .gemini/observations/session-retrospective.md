# session-retrospective 知見 (L4: Project)

## 完了の定義 (DoD) とクリーンアップ
- **RED**: セッション終了時に `debug_regex.py` や一時的なテストファイルが残存していた。
- **GREEN**: 完了（DoD）を宣言する前に、必ず `ls` や `git status` を確認し、実装過程で作成したデバッグ用スクリプトや一時ファイルを物理的に削除せよ。
