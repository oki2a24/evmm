# WBS/EVM マスターテンプレート生成 実装計画 (詳細要件 + TDD)

> **AIエージェントへの指示:** REQUIRED SUB-SKILL: この計画をタスクごとに実装するには、`subagent-driven-development` スキル（推奨）または `executing-plans` スキルを `activate_skill` で起動して使用してください。ステップには追跡用のチェックボックス (`- [ ]`) を使用します。

**目標:** `docs/plans/2026-05-03-master-template-design.md` に基づき、TDDを用いて自動計算機能付きの WBS/EVM Excel テンプレートを生成する。

**アーキテクチャ:**
- `pytest` をテストフレームワークとして使用。
- `openpyxl` を用いて、スタイル、数式、データバリデーションを含む Excel を生成・検証する。
- `Settings` シートを起点に、名前定義を活用して数式の保守性を高める。

---

### タスク 1: テスト環境構築と共通スタイルの定義

**ファイル:**
- `tests/test_template_generator.py` (作成)
- `scripts/generate_master_template.py` (作成)

- [ ] **ステップ 1: 最初の失敗するテストを作成**
  - ファイル `templates/master_template.xlsx` の存在を確認するテスト。
- [ ] **ステップ 2: テストの失敗を確認**
- [ ] **ステップ 3: スクリプトの骨格実装**
  - 空の Excel ファイルを指定パスに保存。共通スタイル（ヘッダー用、計算式用、アラート用）の定義クラスを用意。
- [ ] **ステップ 4: テストの成功を確認**
- [ ] **ステップ 5: コミット**

### タスク 2: `Settings` シートと名前定義の実装

- [ ] **ステップ 1: 設定項目の検証テスト作成**
  - `Settings` シートの存在、および「プロジェクト開始日」「メンバーリスト」等の見出し位置を確認するテスト。
- [ ] **ステップ 2: テストの失敗を確認**
- [ ] **ステップ 3: `Settings` シートの実装**
  - プロジェクト基本情報、祝日リスト、メンバーリストの入力エリアを作成。
  - **名前定義の活用**: 後の数式で使いやすいよう、メンバーリストなどの範囲に Excel の「名前」を定義。
- [ ] **ステップ 4: テストの成功を確認**
- [ ] **ステップ 5: コミット**

### タスク 3: `WBS_EVM` シートの構造（3フェーズ）実装

- [ ] **ステップ 1: ヘッダー構造の検証テスト作成**
  - 「作成」「レビュー実施」「レビュー後修正」がセル結合（Merged Cells）されているか、日本語併記が正しいかを検証。
- [ ] **ステップ 2: テストの失敗を確認**
- [ ] **ステップ 3: `WBS_EVM` シートの生成実装**
  - 3フェーズ構造をセル結合で再現。
  - 開始/終了（予定・実績）、工数、進捗率の列を配置。
- [ ] **ステップ 4: テストの成功を確認**
- [ ] **ステップ 5: コミット**

### タスク 4: EVM 自動計算式とアラートの実装

- [ ] **ステップ 1: 計算式と条件付き書式の検証テスト作成**
  - PV/EV/AC の数式が正しいか、SPI/CPI のアラート設定（0.9未満で赤）が存在するかを検証。
- [ ] **ステップ 2: テストの失敗を確認**
- [ ] **ステップ 3: 計算式とアラートの埋め込み実装**
  - 日付と進捗率から各指標を算出する Excel 数式を全行に展開。
  - 用語解説をセルコメントとして追加。
- [ ] **ステップ 4: テストの成功を確認**
- [ ] **ステップ 5: コミット**

### タスク 5: `チームEVM` 集計シートの実装

- [ ] **ステップ 1: 集計テーブルと数式の検証テスト作成**
  - チーム別の集計ブロックが存在し、`SUMIFS`/`COUNTIFS` を含む数式が設定されているかを検証。
- [ ] **ステップ 2: テストの失敗を確認**
- [ ] **ステップ 3: チーム集計シートの実装**
  - メンバーリストからリーダーを抽出し、動的に集計ブロックを生成。
  - `WBS_EVM` シートから条件（リーダー、フェーズ、日付）に一致するデータを集計する数式を埋め込む。
- [ ] **ステップ 4: テストの成功を確認**
- [ ] **ステップ 5: コミット**

---
## セルフレビュー
1. **仕様の網羅性**: 3フェーズ、チーム集計、名前定義、日本語併記、アラートがすべてテストと実装に含まれているか？ -> YES
2. **TDDの規律**: すべてのステップが テスト作成 -> 失敗確認 -> 実装 -> 成功確認 のサイクルになっているか？ -> YES
3. **型の整合性**: openpyxl でのセル番地計算に誤りはないか？ -> テストコードでの座標検証により保証。
